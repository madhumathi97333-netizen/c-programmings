#include <stdio.h>

int main() {
    int CP, SP;
    scanf("%d %d", &CP, &SP);
    double profit = SP - CP;
    double profit_percentage = (profit / CP) * 100;
    printf("%.2f\n", profit_percentage);
    return 0;
}

